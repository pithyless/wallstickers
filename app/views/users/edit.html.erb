<h1>Editing user</h1>

<%= semantic_form_for @user, :url => edit_user_path(@user) do |f| %>
  <%= f.inputs :first_name, :last_name, :password %>
  <%= f.input :password_confirmation, :as => :password %>
  <%= f.buttons do %>
    <%= f.commit_button %>
  <% end %>
<% end %>